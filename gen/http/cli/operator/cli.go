// Code generated by goa, DO NOT EDIT.
//
// operator HTTP client CLI support package
//
// Command:
// $ goa gen github.com/eclipse-xfsc/kubernetes-operator/design -o .

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	inventoryc "github.com/eclipse-xfsc/kubernetes-operator/gen/http/inventory/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"inventory (version|modules|types|providers|consumers|injections|accounts|accounts-by-consumer|manifests-requesting-injection|healthz|readyz)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "inventory version" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		inventoryFlags = flag.NewFlagSet("inventory", flag.ContinueOnError)

		inventoryVersionFlags = flag.NewFlagSet("version", flag.ExitOnError)

		inventoryModulesFlags = flag.NewFlagSet("modules", flag.ExitOnError)

		inventoryTypesFlags = flag.NewFlagSet("types", flag.ExitOnError)

		inventoryProvidersFlags = flag.NewFlagSet("providers", flag.ExitOnError)

		inventoryConsumersFlags = flag.NewFlagSet("consumers", flag.ExitOnError)

		inventoryInjectionsFlags = flag.NewFlagSet("injections", flag.ExitOnError)

		inventoryAccountsFlags = flag.NewFlagSet("accounts", flag.ExitOnError)

		inventoryAccountsByConsumerFlags         = flag.NewFlagSet("accounts-by-consumer", flag.ExitOnError)
		inventoryAccountsByConsumerNamespaceFlag = inventoryAccountsByConsumerFlags.String("namespace", "REQUIRED", "")
		inventoryAccountsByConsumerNameFlag      = inventoryAccountsByConsumerFlags.String("name", "REQUIRED", "")

		inventoryManifestsRequestingInjectionFlags = flag.NewFlagSet("manifests-requesting-injection", flag.ExitOnError)

		inventoryHealthzFlags = flag.NewFlagSet("healthz", flag.ExitOnError)

		inventoryReadyzFlags = flag.NewFlagSet("readyz", flag.ExitOnError)
	)
	inventoryFlags.Usage = inventoryUsage
	inventoryVersionFlags.Usage = inventoryVersionUsage
	inventoryModulesFlags.Usage = inventoryModulesUsage
	inventoryTypesFlags.Usage = inventoryTypesUsage
	inventoryProvidersFlags.Usage = inventoryProvidersUsage
	inventoryConsumersFlags.Usage = inventoryConsumersUsage
	inventoryInjectionsFlags.Usage = inventoryInjectionsUsage
	inventoryAccountsFlags.Usage = inventoryAccountsUsage
	inventoryAccountsByConsumerFlags.Usage = inventoryAccountsByConsumerUsage
	inventoryManifestsRequestingInjectionFlags.Usage = inventoryManifestsRequestingInjectionUsage
	inventoryHealthzFlags.Usage = inventoryHealthzUsage
	inventoryReadyzFlags.Usage = inventoryReadyzUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "inventory":
			svcf = inventoryFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "inventory":
			switch epn {
			case "version":
				epf = inventoryVersionFlags

			case "modules":
				epf = inventoryModulesFlags

			case "types":
				epf = inventoryTypesFlags

			case "providers":
				epf = inventoryProvidersFlags

			case "consumers":
				epf = inventoryConsumersFlags

			case "injections":
				epf = inventoryInjectionsFlags

			case "accounts":
				epf = inventoryAccountsFlags

			case "accounts-by-consumer":
				epf = inventoryAccountsByConsumerFlags

			case "manifests-requesting-injection":
				epf = inventoryManifestsRequestingInjectionFlags

			case "healthz":
				epf = inventoryHealthzFlags

			case "readyz":
				epf = inventoryReadyzFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "inventory":
			c := inventoryc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "version":
				endpoint = c.Version()
			case "modules":
				endpoint = c.Modules()
			case "types":
				endpoint = c.Types()
			case "providers":
				endpoint = c.Providers()
			case "consumers":
				endpoint = c.Consumers()
			case "injections":
				endpoint = c.Injections()
			case "accounts":
				endpoint = c.Accounts()
			case "accounts-by-consumer":
				endpoint = c.AccountsByConsumer()
				data, err = inventoryc.BuildAccountsByConsumerPayload(*inventoryAccountsByConsumerNamespaceFlag, *inventoryAccountsByConsumerNameFlag)
			case "manifests-requesting-injection":
				endpoint = c.ManifestsRequestingInjection()
			case "healthz":
				endpoint = c.Healthz()
			case "readyz":
				endpoint = c.Readyz()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// inventoryUsage displays the usage of the inventory command and its
// subcommands.
func inventoryUsage() {
	fmt.Fprintln(os.Stderr, `Service is the inventory service interface.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] inventory COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    version: Version implements version.`)
	fmt.Fprintln(os.Stderr, `    modules: Modules implements modules.`)
	fmt.Fprintln(os.Stderr, `    types: Types implements types.`)
	fmt.Fprintln(os.Stderr, `    providers: Providers implements providers.`)
	fmt.Fprintln(os.Stderr, `    consumers: Consumers implements consumers.`)
	fmt.Fprintln(os.Stderr, `    injections: Injections implements injections.`)
	fmt.Fprintln(os.Stderr, `    accounts: Accounts implements accounts.`)
	fmt.Fprintln(os.Stderr, `    accounts-by-consumer: AccountsByConsumer implements accountsByConsumer.`)
	fmt.Fprintln(os.Stderr, `    manifests-requesting-injection: ManifestsRequestingInjection implements manifestsRequestingInjection.`)
	fmt.Fprintln(os.Stderr, `    healthz: Healthz implements healthz.`)
	fmt.Fprintln(os.Stderr, `    readyz: Readyz implements readyz.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s inventory COMMAND --help\n", os.Args[0])
}
func inventoryVersionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] inventory version", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Version implements version.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "inventory version")
}

func inventoryModulesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] inventory modules", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Modules implements modules.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "inventory modules")
}

func inventoryTypesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] inventory types", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Types implements types.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "inventory types")
}

func inventoryProvidersUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] inventory providers", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Providers implements providers.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "inventory providers")
}

func inventoryConsumersUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] inventory consumers", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Consumers implements consumers.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "inventory consumers")
}

func inventoryInjectionsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] inventory injections", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Injections implements injections.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "inventory injections")
}

func inventoryAccountsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] inventory accounts", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Accounts implements accounts.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "inventory accounts")
}

func inventoryAccountsByConsumerUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] inventory accounts-by-consumer", os.Args[0])
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `AccountsByConsumer implements accountsByConsumer.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)
	fmt.Fprintln(os.Stderr, `    -name STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "inventory accounts-by-consumer --namespace \"Officiis expedita omnis reprehenderit.\" --name \"Magni autem consequatur voluptatem ipsum ex voluptas.\"")
}

func inventoryManifestsRequestingInjectionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] inventory manifests-requesting-injection", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `ManifestsRequestingInjection implements manifestsRequestingInjection.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "inventory manifests-requesting-injection")
}

func inventoryHealthzUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] inventory healthz", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Healthz implements healthz.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "inventory healthz")
}

func inventoryReadyzUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] inventory readyz", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Readyz implements readyz.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "inventory readyz")
}
